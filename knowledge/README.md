# knowledge/ — 過去コンテストの分析とテクニック（現在のコンテストで使うための置き場）

**このフォルダはテンプレに同梱される** ＝ `new.sh` で作った新しいコンテスト repo にそのままコピーされる。
だから**コンテスト開始時に「過去の自分の分析」が手元にある**。

```
knowledge/
├── README.md          この文書（構造カルテの軸・書き方・索引）
├── contests/          参加記録: 1回1ファイル。結果論ベースの構造分析
├── methods/           **大枠の手法**: ビーム / SA / 時空間BFS …。**この一覧が「手持ち」そのもの**
└── techniques/        テクニック: 手法の中で使う部品。1件1ファイル
```

**`methods/` と `techniques/` の違い**: 手法は**解き方の枠組み**（何を状態とし、何を最適化するか。
これを選ぶと他が決まる）。テクニックは**枠組みの中の部品**（単体では解法にならない）。
迷ったら「これ単体で解答を最後まで作れるか」で判定する。

## 正本はどこか

- **正本は `~/ahc/template/knowledge/`。** コンテスト repo 側にあるのは**コピー（読み取り専用）**。
- **コンテストが終わったら、参加記録とテクニックを書いて `template/knowledge/` に置く**（正本を更新する）。
  コンテスト repo 側のコピーを編集しても次に伝わらない。

## 3層の棲み分け

| 層 | 場所 | 中身 | 性質 |
|---|---|---|---|
| ルール | `CLAUDE.md` | **毎回必ず従うこと**（一般化済み） | 恒久・短い・根拠は1行 |
| 分析 / 道具 | **`knowledge/`** | **構造カルテと結果論の分析 / 手法 / 適用条件つきの技術** | 蓄積・詳しい |
| 一次資料 | 各コンテスト repo の `docs/` | 生ログ（測定値・経緯・全アプローチ・提出の推移） | private repo に残る |

- **同じ知見が2回目のコンテストで出たら `CLAUDE.md` のルールに昇格させる**（1回だけならここに置く）。
  昇格したらこちら側に「→ CLAUDE.md 済」と印をつける（どちらが正かを迷わないため）。

---

## 問題の構造カルテ（8軸・9欄。軸3 だけ 2 段）

**新しい問題を読んだら、まずこれを埋める。** 目的は「この問題の構造では**どの解法クラスが勝ち、どれが相性が
悪いか**」を、コードを書く前に読み取ること。

> **軸の受入テスト**: AHC068 の構造をこのカルテに書いたとき、**1行もコードを書く前に**
> 「距離ベースの評価関数＋隣接スワップの探索は負ける」と読めること。読めない軸は役に立たない。
> **AHC069 版**: 同じく着手前に「**矩形だけの形族では大口（`P ≥ 81`）は届かない**」と読めること
> ── これは軸3a（条文）では読めず、**軸3b(b)（入力の端）を測って初めて読める**。新しい軸を足すときは、
> **過去の失敗がその軸で捕まるか**を必ず当てる。

**カルテは一度に埋まらない。埋まる順に3段ある**（埋まっていない軸を空欄のまま放置しないための目印）:

| いつ埋まるか | 軸 |
|---|---|
| **問題文だけで埋まる**（着手前・ここが本番） | **1 出力の型 / 2 スコアの単位 / 3a 1手の威力 / 3b(a) 条文の端 / 5 決定の干渉 / 7 入力の乱雑さ** |
| 安い測定で埋まる（実行可能解ができた直後） | 🔴 **3b(b) 入力の端**（入力を1回走査するだけ。**ahc069 はここを 10 日落とした**）/ **6 ケース分布**（seed 掃引の分布）/ **8 余裕と感受性**（TL を N 倍して2回走らせる） |
| 順位表が出てから埋まる | **4 分解と結合**（独立解の天井 ⇔ 上位スコア。天井の計算自体は軸7 が埋まれば着手前にできる） |

⇒ **軸4 は「天井を先に計算しておき、上位スコアが見えた時点で突き合わせる」**のが正しい使い方。
ahc068 では天井（2.345手/枚 = est 1011M）を着手前に出せたのに計算しておらず、上位と比べる機会を逃した。

### 出力とコスト（ここで解法クラスがほぼ決まる）

> **最終列「自分の実装」は空欄で放置しない。** カルテは*問題*を記述するが、**過去 2 回の一番の失敗は 2 回とも
> *実装*の側にあった**（ahc068: 配達を距離最短で実装 / ahc069: fit 条件を外接矩形全部空きに）。
> **どちらもカルテは正しく埋まっていた。** ⇒ 各軸に「その軸に対して自分が何を選んだか」を紐づけ、
> `docs/NOW.md` の register に落とす。**レバーが全滅したら、まずこの列を上から疑う。**

| # | 軸 | 見るもの | 何が決まるか | 自分の実装（→ register） |
|---|---|---|---|---|
| 1 | **出力の型** | 配置・割当 / 操作列（時系列）/ 対話 | 配置なら SA・局所探索が素直に乗る。操作列は状態爆発するので「1つずつ確定する構築」が強い。対話は情報収集と方針切替 | 解を何で表すか |
| 2 | **スコアの単位** | **(a) 1ケース内**: 何が点になるか（品質 / 回数 / 誤差）。log か線形か。**手元で最小化する代理指標と、点になる量が同じ単位か**<br>**(b) ケース間の集計**: 絶対和 / **相対（`1e9 × 自分/全参加者最大`）** / log | (a) 回数が点なら手数最小化ゲーム。**代理指標の単位がずれていたら、そこで打ち切る**<br>🔴 (b) が**判定プロトコルを丸ごと決める** ── 相対評価なら**絶対スコア和で採否を決めてはいけない**（`CLAUDE.md` の「相対評価のコンテストでの判定」節へ） | 代理指標は何か / 採否をどの統計で見るか |
| 3a | **1手の威力と副作用** | 1手の到達範囲（距離に依存する/しない）。巻き込み（害か**資源**か）。代数的性質（対合・可換・群） | 距離非依存なら距離ベースの評価は無効。巻き込みが資源なら**共有**が主レバー。対合なら操作列を可塑に保てる | 1手をどう実装したか |
| 3b | 🔴 **1手の可否（前提）** | **(a) 条文の端**: パラメータの端（幅1 / 最小サイズ / 上限いっぱい / degenerate）が合法か<br>**(b) 入力の端**: 入力を 1 回走査して分かる幾何的な上限（例: 全部芝生の最大正方形） | **条文が許す*最も強い操作*を primitive にし、条文が要求する*最も弱い条件*を可否の判定式にする。**<br>(b) が「その手を**そもそも到達可能か**」を決める | **可否の `if` そのもの**（ここが register の行） |

**なぜ 3 を割るか（受入テストに落ちたから）**: 上の受入テストを ahc069 で当てると
「カルテを書いたら『**矩形だけの形族では大口は届かない**』と読めるか」になるが、**読めなかった**。
軸3 が**条文の側だけ**を向いていて、効いたのは**入力の幾何**（全部芝生の最大正方形が 6〜8 ⇒ `P ≥ 81` は
空の盤面でも `L_min` 不可）だったため。**入力を 1 回スキャンすれば着手前に分かった**のに、測ったのは 10 日後。
ahc068 の失敗（`w=1` の見落とし）は (a)、ahc069 は (b) ⇒ **同じ軸の別の半分を 1 回ずつ落としている。**
⚠ 番号を振り直さないのは、既存の「軸1〜3で解法クラスが決まる」と 30 箇所以上の軸4〜8 参照を壊さないため。

### 問題の結合

| # | 軸 | 見るもの | 何が決まるか | 自分の実装（→ register） |
|---|---|---|---|---|
| 4 | **分解と結合** | 独立な部分問題に切れるか。切ったときの**理論下限は上位スコアに届くか** | 「**分解できるが、分解したら数学的に届かない**」を検出する軸。届かないなら結合が必須 | どこで切ったか |
| 5 | **決定の干渉** | 確定したものが後の自由度を削るか。実行可能性は局所判定か大域か。**干渉を有料で巻き戻せるか** | 削るなら**順序**がレバー。大域制約なら連結性維持などの構造が必要。**巻き戻せるなら「守る」より「詰まってから作り直す」が正しい**（ahc069: 断片化を守る系のレバーが 6 本全滅した理由） | 順序の決め方 / 巻き戻しの実装 |

### 入力と資源

| # | 軸 | 見るもの | 何が決まるか | 自分の実装（→ register） |
|---|---|---|---|---|
| 6 | **ケース分布** | 何が難易度を決めるか。極端なケースで最適戦略が変わるか | 変わるなら戦略分岐、変わらないなら単一戦略に集中 | 分岐したか / しなかったか |
| 7 | **入力の乱雑さ** | 活用できる偏り・構造（クラスタ / 周期 / 幾何的近接）があるか。一様ランダムか。**生成則が公開されているか** | 構造があれば前処理・分解・パターン利用が効く。一様ランダムなら**期待値・下限の解析**ができる（軸4 に使う）。**生成則が公開なら到着済みから分布をオンライン推定できる**（ahc069 の受入制御） | 推定器を持つか |
| 8 | **制約の余裕と感受性** | **余裕**: TL/メモリに対し1回の構築・評価が何回まわるか。**感受性**: TL を N 倍したらスコアがどれだけ伸びるか（安く実測できる） | **この2つは別物。**余裕があっても感受性が低ければ、多スタートや深い探索は買えない ⇒ 1回の質（構築・データ構造）が支配的。SA/ビームは感受性が高い前提の道具 | 何に時間を使っているか |

### 🔴 軸4 と軸8 は「値」ではなく「測った日 / 版 / 再測定トリガ」を書く

この 2 軸だけは**版が進むと値が動く**。1 個の数字を書いて放置すると、**古い天井で投資判断をする**ことになる。

| | ahc069 で実際に動いた例 |
|---|---|
| 軸4（天井） | ビームの天井 **+2.003%** → 機構を積んだ後に測り直したら **+0.278%**（**86% が既に回収済み**だったのに、古い値のまま提案した） |
| 軸8（感受性） | 候補数の感受性は序盤 **+3.956%**（blob の種 16→256）→ 終盤は **±0.1%**（飽和。増やしても減らしても動かない） |

⇒ 書式は `値（測定日 / 版 / 再測定トリガ）`。**再測定トリガ**の例: 「新しい候補生成を入れたら」「速度が 2 倍になったら」。
**未実装のまま温めている候補の天井も、別の機構を入れたら測り直す。**

### 「状態・遷移・評価」の位置

**これは9番目の軸ではない。** 軸1〜3で枠組み（＝`methods/` のどれか）を選んだ**後**に、
**その手法の記事の「中身」欄**に書くもの。カルテの段で先に書くと「探索で解く」という前提が入る
（**この3欄はきれいに埋まったまま、何の警告も出さない** ── AHC068 で負けた枠組みは
「状態=盤面 / 遷移=隣接スワップ / 評価=距離和」で完全に記述でき、しかも何も警告しなかった）。

---

## 手法の書き方（`methods/<name>.md`）

**「ビームサーチとは」を説明する記事ではない**（それは他所にある）。**自分が使えるようにするための記事**。

1. **一言で**（何を最適化する手法か）
2. **他の手法との対比** ── 特に**何を最適化するか / 状態 / 方向を決めるもの / 時間を増やすと伸びるか**。
   ここが一番役に立つ（別の族に迷い込んだ時に気づける）
3. **いつ使うか** ── カルテの軸で。**向かない条件も書く**
4. **中身**（状態・遷移・評価。ここが「状態・遷移・評価」の正しい置き場）
5. **骨格**（漸化式・擬似コード。次回そのまま写せる粒度で）
6. **実装の要点 / 調整点**（env で外出しするノブと、掃引で決まった値）
7. **落とし穴**（自分が踏んだもの）
8. **実績**（どのコンテストで、どれだけ）
9. **別名・関連**（一般的な呼称があれば。検索できるようにする）

## 参加記録の書き方（`contests/ahcNNN.md`）

**結果論ベースで書く。試行錯誤の経過・提出スコアの推移・後から入手した解説は載せない**（それらは一次資料 or
techniques/ 側）。書くのは「**問題がこういう構造だったから、この解法が上位を取れた／これは相性が悪かった**」。

1. **問題の構造カルテ**（上の8軸・9欄）
2. **上位が取れた理由** — カルテのどの軸の帰結としてその解法が強いのか
3. **相性が悪かったもの** — どの軸に反していたのか（自分が採った道もここに入れる）
4. **結果** — 最終順位とスコア、上位との差（推移は一次資料へ）
5. **この回で得た道具** → `methods/` / `techniques/` へのリンク

## テクニックの書き方（`techniques/<name>.md`）

- **主張**（1行で何ができるか）
- **適用条件** — **構造カルテの軸の値で書く**（例:「軸1=操作列」かつ「軸3の代数=対合」）。
  これがあると、新しい問題のカルテを埋めた時点で**使える道具が引ける**。
- **効果** — どのコンテストで、どれだけ（測定値）
- **限界・反例** — 効かなかった条件
- **昇格状況** — `CLAUDE.md` のルールにしたなら「→ CLAUDE.md 済」

---

## 索引：参加記録

| 回 | 問題の型（軸1/2/3） | 結果 | 構造の答え | 一番の失敗 | 記録 |
|---|---|---|---|---|---|
| AHC069 | **対話＋オンライン配置** / 金額和（**相対評価**）/ **1手 = `V·C`。`C` は幾何だけで決まり、`L_min` は大口では原理的に不可能** | 89.612%（**153位/911人**）。**首位 99.063% / 5位 98.010% / 50位 94.100%** | **任意形を安く大量に列挙する幾何機械**（形カタログ ＋ bit 一括列挙 ＋ Window Packing ＋ 周長の局所改善）。受入制御は主因ではない（自分の分位規則も頂点だった） | **「外接矩形が全部空き」という fit 条件を 10 日間疑わなかった**（緩めるだけで +1.19%。ahc068 と同じ型 ＝ 実装上の選択が問題の性質の顔で居座る） | [`contests/ahc069.md`](contests/ahc069.md) |
| AHC068 | 操作列 / 操作回数 T の log / **1手の威力が距離にほぼ非依存・巻き込みは資源・対合** | 920.7M（222位）、3.55手/枚。**5位以内は 2.18手/枚以下** | 手数最短の構築 ＋ **相乗り**（独立配達の下限 2.345手/枚 を上位が割っていた） | **軸2・軸3 を検査せず、距離ベースの代理指標で探索へ進んだ**（配達の実装が「距離最短」だと最後まで気づかず、代償 3.9倍） | [`contests/ahc068.md`](contests/ahc068.md) |

## 索引：手法（**手持ちの一覧**。新しい問題では、カルテを埋めてからここを見て選ぶ）

| 手法 | 何を最適化するか | 状態 | 効く条件（軸） | 記事 |
|---|---|---|---|---|
| ビームサーチ | **解の質**（評価関数を登る） | 解の候補 | 軸1=配置/割当 / 軸2=品質 / **軸8の感受性が高い**（反復が伸びに変わる） | 未記述（手持ち） |
| 焼きなまし（SA） | 同上 | 解の候補 | 上記＋**近傍が安く差分評価できる** | 未記述（手持ち） |
| chokudai サーチ | 同上（幅を時間で稼ぐ） | 解の候補 | 上記と同じ。**AHC068 では退行**（916.7M vs ビーム 920.7M）＝ 感受性が低い問題では効かない | 未記述（手持ち） |
| **時空間BFS** | **出力列の長さ**（回数そのもの） | **(1対象の位置 × 操作列の時刻)** | 軸1=**操作列** / 軸2=**回数** / 軸3=**1操作が複数対象を動かす** | [`methods/spacetime-bfs.md`](methods/spacetime-bfs.md) |

- **評価関数を登る族（上3つ）と、出力列の長さを削る族（時空間BFS）は別のゲーム。**
  前者は「反復を増やすと伸びる」前提の道具なので、**軸8の感受性が低い問題に持ち込むと空振りする**
  （AHC068 で実際に空振りした）。
- 手法を1つ試すたびに、**効かなかった条件も**この表に書く（ビーム系が AHC068 で効かなかったことも情報）。

## 索引：テクニック

| 道具 | 主張 | 効く条件（軸） | 昇格 |
|---|---|---|---|
| [相乗り](techniques/ride-along.md) | 1操作の副作用を資源として複数対象で共有する（追加0操作） | 軸1=操作列 / 軸3の巻き込みが大 / **軸4で独立解の下限が上位に届かない** | 未 |
| [逐次適用をDPに統合](techniques/unify-sequential-dp.md) | 段を分けると食い合う。1つの値関数にまとめる | 軸1=操作列 / 各手段が同じ資源を書き換える / **単独では正なのに合計が小さい** | 未（一般性は高そう） |
| [自由度管理](techniques/freedom-set.md) | 手を確定させず「同じ効果を与える手の集合」で持ち、要求が来たら絞る | 軸3に自由度がある / 軸5に干渉がある / 相乗りと組む | 未 |
| [事前に狭める](techniques/narrow-before-verify.md) | 作って却下するのではなく、候補生成の段で制約を張る | 軸5に干渉がある / **却下率が高い**（先に内訳を数える） | 一部 → CLAUDE.md 済 |
| [測り方の条件が採否を反転させる](techniques/measurement-regime.md) | **時計を外した掃引の値と実 TL の値は別の量**。さらに**計測の並列度そのものが採否を変える**（弁を人工的に発火させる）⇒ 時間を食うレバーは実 TL・低並列・paired で測り直す | **軸8 に「余裕はあるが裾が律速」**と書いたら入る／レバーが時間を食うとき | **→ CLAUDE.md 済** |
| [終了後は順位を直接測る](techniques/post-contest-truth.md) | `ahc_standings` の得点行列で**分母が真値になる** ⇒ 「この版は何位相当か」が推定なしで出る。延長戦の判定はこれ一本 | 終了後（開催中は 403）／相対評価の回で最も効く | **→ CLAUDE.md 済** |
| [核の書き換えは assert で同値を取る](techniques/equivalence-by-assert.md) | **byte 一致は「機構を足す」道具で、核を書き換えると通らない ⇒ 初日の核が凍る**（ahc069 の fit 条件は 124版中123版で不変）。旧実装を残して実走行中に `assert!(旧==新)` | **軸に依らない**（プロセスの道具）。同じ量を 2 通りで計算できること | **→ CLAUDE.md 済** |
| [bit 盤面で置ける位置を一括列挙](techniques/bitboard-enumeration.md) | 幅≤64 なら 1行=1語。`f &= f>>k` で「L 連続空きの開始位置」を作り、形の各行ぶんシフトして AND ⇒ **置ける `y` が 1 本のマスクで出る**。**サンプリング探索を総当たりに変えられる** | 軸1=配置/割当 / **軸3が矩形に限らない**（矩形だけなら累積和で足りる）/ **軸8の候補数感受性が残っている** | 未（実績0。ahc069 の上位 2 解法が採用） |
| [近傍ではなく提案分布を狙う](techniques/targeted-proposal.md) | 近傍集合は足りていて、乱択が当たりを引く確率だけが低いことがある ⇒ **欲しい結果から手を逆算して提案分布に混ぜる**。`ask/hit/acc` を数えて律速を切り分ける | 軸3a=1手が局所操作で効果は大域 / 軸5に干渉 / **軸8の感受性が低い**（反復が伸びに変わらない） | 未 |
| [原子的な複合手](techniques/atomic-two-stage-move.md) | **①必ず下がる → ②上がる** の 2 段の利得は、温度・門・提案分布では絶対に通らない ⇒ **「壊す手 ＋ 直す手」を対で設計し、最終状態だけを受理規則に掛ける** | 軸2=積 or 重み付き和 / 軸5に干渉 / **制約を保つ操作が解を丸ごと不変にする族** | 未 |
| [「効かなかった」の切り分け](techniques/mechanism-failure-triage.md) | 新機構が null の原因を **実装 / 置き場所 / 供給** に割る。**②③は差分に出ない**ので A/B を何本回しても見えない | 軸1=配置/割当 で新しい近傍を足したのに null / 軸5に干渉 | **→ CLAUDE.md 済** |
| [目的関数の項の寄与を測る](techniques/objective-term-share.md) | **各項が全体の何 % か**を初日に印字する。**個数は見えやすく、重み付き和は見えにくい** ⇒ 見えやすい方だけ最適化する事故が起きる | **軸2(a) が「品質 × 個数」「Σ 重み × 量」**／ 重みが要素ごとに違うとき必須 | 未 |
| [帯の監査](techniques/band-audit.md) | 場合分けの帯を入れたら**全レバー × 全クラスの表**を作り、**「根拠が空のセル」を探す**。空セルは新機構より安く埋まる | クラス別に設定を変える設計にしたとき常に | 未 |
| [`.max()`/`.clamp()` はレバーを殺す](techniques/silent-clamp.md) | 床・天井に飲まれた env は**存在しないのに掃引は `null` としか出さない**。過去の掃引の解釈まで遡って壊れる ⇒ **投入時に「実際に何になったか」を印字** | **軸に依らない**（実測値から自動計算する量に安全弁を付けたとき危険） | **→ CLAUDE.md 済** |
| [相対評価の順位表を読む](techniques/relative-leaderboard-reading.md) | **分母のドリフトは非提出者から直接測れる**（定数ではない）／ 転写率は自分で測る ／ **「首位比 9 割」は錯覚 ⇒ 差は「自分の開発量」と「順位密度」の単位で読む** | **軸2(b) が相対評価** | 一部 → CLAUDE.md 済 |
| 🔴 [積の目的関数を 2 段に割る](techniques/two-phase-product-objective.md) | `A × B` を同時に登ると**トレードオフ曲線を滑るだけ**になる ⇒ **①A を最大化 → ②A を不変に保って B を最適化**。設計時に「**A を固定したまま B だけ動かす手はあるか**」を問う | **軸2 が積 or 重み付き和**で因子が資源を奪い合う / 軸5に干渉 / **片方を上げる手ばかり手元にあるとき** | 未 |
| 🔴 [保存量なら最適化は配分問題](techniques/conserved-quantity-partition.md) | `Σ 重み × 量` で**総量が条文から定数**なら、稼ぐゲームではなく**どの重みの下に置くかの配分**。⇒ **「あえて減らす」手が正になりうる**。削ってよい**床**を幾何・条文から出す | **軸2 が `Σ 重み × 量`** / **軸4 で全資源がちょうど 1 要素に属する**（分割・彩色・マッチング・経路分解）| 未 |
| 🔴 [合成して不変量に戻す](techniques/compose-into-invariant.md) | 1 手が**置換**を起こすなら、**合成して恒等に戻す閉路**を探す（k-opt 型）。**単発の符号で近傍を否定しない**。⇒ 補題は「壊れるもの」と「**保たれるもの**」を両方書く | 軸3a=1手が局所操作で効果は大域 / 軸5に干渉 / **制約を保つ操作が解を丸ごと不変にする族** | 未 |
| [部分問題に既存分野の名前を付ける](techniques/name-the-subproblem.md) | 「配線 / マッチング / 被覆 / スケジューリング」と名前が付けば標準アルゴリズムが棚から取れる。**自作の近傍を書き始める前にやる** | **軸に依らない**（分解した直後に 1 回）| 未 |
| [空振り率が暗黙の弁になる](techniques/implicit-throttle.md) | `if 当たった { 素の近傍を skip }` は**率を暗黙に持つ** ⇒ 命中率を上げると探索が死ぬ（受理率 0.977→0.012 の実績）。**率を env に出して密度を揃えてから質を測る** | **軸に依らない**。狙い撃ち・優先度付き提案・候補プールを入れたとき常に | 未 |
| [終盤の打ち切りは掃引で決める](techniques/endgame-gate.md) | **「終盤は探索が無駄」は ahc068 では逆だった**（切らない方が +52M）。閾値は env で外出しして掃引する | 軸1=操作列 / 軸5に干渉がある（**この構造こそが直感を生むので必ず疑う**） | 一部 → CLAUDE.md 済 |
