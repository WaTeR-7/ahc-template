# AHC<XXX> — 変更履歴（1コミット1行）

> 盤面（[`NOW.md`](NOW.md)）から分けてある台帳。**最新を末尾に。時刻はすべて JST。**
> 🔴 **メッセージに関係する ID（`NG07` / `DC05b` / 版のファイル名）を書く**
> ⇒ `git log --grep` と `grep -rn` が同じ住所で引ける。

| コミット | 内容 |
|---|---|
| `<hash>` | 初期セットアップ（テンプレから scaffold）。 |
