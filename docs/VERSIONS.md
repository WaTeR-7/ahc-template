# AHC<XXX> — 版の一覧（台帳）

> 盤面（[`NOW.md`](NOW.md)）から分けてある台帳。**盤面は毎セッション読むので台帳を混ぜない。**
> 役目を終えた版は `../attic/`。🔴 **提出した版のファイルは以後 read-only**（比較の基準が消えるため）。

| ファイル（＝ ID） | アプローチ / 何を足したか | env | ローカル結果 | 判定 |
|---|---|---|---|---|
| `00_base.rs` | ベースライン（構築のみ） | — | — | — |

- 🔴 **版に通番を振らない。ファイル名がそのまま ID**（別名を作ると二重管理になる。`../CLAUDE.md §1`）。
- 🔴 **1アプローチ = 1物理ファイル。過去の版を絶対に上書きしない。新しいレバーの既定は OFF。**
- `判定` が負のものは **`NOW.md` の否定の棚へ**（前提つきで書く。負のまま流すと死蔵になる）。
- 結果は `results.meta` の `bin / env / seeds` ごと引用する（比較の作法は `../CLAUDE.md`）。
