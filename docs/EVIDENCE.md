# AHC<XXX> — 証拠（測定の全記録）

> **これは「なぜそう言えるか」を確かめるとき用。** 盤面は [`NOW.md`](NOW.md)、前提は [`SETUP.md`](SETUP.md)。
> 🔴 **append-only** ── 一度書いた測定は書き換えない（訂正は新しい `EX###` として足す）。
> 🔴 **測定 1 本ごとに `EX001` から通番を振る**（見出しに入れる。`## EX007 箱の天井（dev 300・実TL・JOBS=3）`）。
> **族には紐付けない** ── 育つと `evidence/<族>.md` へ割ることになるが、**`EX###` は場所に依存しないので
> 付け替えても壊れない**（族を符号化した ID は再配置で壊れ、しかも書く瞬間に「どの族か」の判断を強制する）。
> `NG` の棚と register からこの ID で引く ⇒ **`grep -rn EX007 .` で測定条件がそのまま出る。**
>
> 🔴 **1000 行を超えたら族ごとに `evidence/<族>.md` へ割り、索引を `evidence/README.md` に置いてこのファイルは消す**
> ── **1 つのファイルに「族順」と「append-only」を同時に課すと両立しない**（`../CLAUDE.md §1`）。

## 主要な発見（引き継ぎ用の要約。詳細は下の `EX###`）

- <何がボトルネックで、なぜ。>

## EX001 <測定の名前>（seed 集合 / env / 並列数 / 基準の版）

- <結果。paired なら `Δrel` と `Δlog` ± se、**勝敗の件数**、`> 2se` か。弁の発火率も併記。>

## 実ジャッジ校正（提出のたびに追記）

| 提出 | 日時(JST) | 版 | ローカル est | 実ジャッジ | 乖離 | ローカル maxms | 実 ms |
|---|---|---|---|---|---|---|---|
| | | | | | | | |

- **est の絶対値は系統的にずれるが、差分は忠実に転写される**ことが多い。3回たまれば「est − N」の形で
  次の提出の見込みが立つ。乖離が急に変わったら、まず**測定条件（seed集合/env/TL）のずれ**を疑う。
- 🔴 **ローカル ms とジャッジ ms は「比」ではなく `加算オーバーヘッド + 係数` で結ぶ**（2点以上で最小二乗）。
  **仕事の種類が変わる機構を入れたら取り直す。**
- 🔴 **提出画面は採否の判定に使えない**（相対評価ではノイズが大きい）。**採否はローカルの paired 統計だけで決め、
  提出は校正専用にする**（`../CLAUDE.md`）。
