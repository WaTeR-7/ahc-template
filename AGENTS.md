# AGENTS.md — Codex / 他エージェント向けの入口

このファイルは**入口だけ**を定義する。**作業ルールの正本は [`CLAUDE.md`](CLAUDE.md)**（リポジトリ直下）。
ルールの本文をここに写さない（二重管理は必ず片方が古くなる）。**唯一の例外が §0 の規約ゲート**で、
これは **fail-closed**（読めていないなら止まる）を成立させるために、**この文書にも機構だけ**明記する。

## 0. AI 利用規約ゲート(最優先・他の何よりも先に)

**正本のテキストは [`problem/ai_guideline.txt`](problem/ai_guideline.txt)**（その回の規約をユーザーが貼り付けた原文）。
**着手前に必ず開いて読む。** ファイルが空 / 未整備なら、

> 本コンテストの AI 利用規約が未読込です(`problem/ai_guideline.txt` が空)。規約を貼り付けてください。

**とだけ出力し、思考・調査・ファイル読み書き・コマンド実行・実装を含む他の一切の挙動を行わず即座に停止する。**
（未読の規約に違反しないための fail-closed。**規約の有無を確認する行為だけが例外**。）

- 読み込まれていれば、**その規約を最優先で順守**した上で `CLAUDE.md` に従う。**矛盾すれば規約が優先。**
- ⚠ **条文をこの文書に写さない。** AHC の AI 利用規約は**回ごとに変わる**ので、写した瞬間に古くなり、
  「ここに書いてある方」を信じた事故が起きる。**読むのは常に `problem/ai_guideline.txt`。**
- 🔴 規約が「実行結果に基づく改善」を制限する版であれば、**運用は「計測 → 報告 → 停止 → 指示を待つ」**になる。
  その場合 `CLAUDE.md` の役割分担（方針はユーザーが握る）と一致するので、**測った結果を見て勝手に次の実装へ
  進まない**ことが規約順守と作業ルール順守を兼ねる。**どこまでが許されるかは毎回その回の条文で確かめる。**

## 1. 読む順番

1. **[`CLAUDE.md`](CLAUDE.md)** — 作業ルールの全文（応答言語・進め方・検証と計測の作法・記録の規律・git）。**必ず全部読む。**
   Codex は `AGENTS.md` を自動で読むが `CLAUDE.md` は自動で読まない ⇒ **このファイルを読んだ時点で `CLAUDE.md` を開く。**
2. **[`NOW.md`](NOW.md)** — このコンテストの記録（用語集 / 序盤チェックリスト / **構造カルテ** / **設計上の選択 register** /
   否定結果の棚 / 変更履歴）。冒頭に「どの節に何を書くか」の対応表がある。
   ⚠ 記録が育つと **`docs/` に 3 分割**される（`NOW.md` = 盤面 / `evidence/` = 証拠 / `SETUP.md` = 前提 / `frozen/` = 凍結スナップショット）。
   その場合は `docs/README.md` から入る。
3. **[`knowledge/README.md`](knowledge/README.md)** の索引 — **手法の索引 ＝ 手持ちの一覧**。
   構造カルテを埋めたら**軸で手法を選ぶ**。型が近い過去回があればその参加記録を読む。

## 2. Codex 側で違うところ(ハーネス差分だけ)

- **`docs/*.md` / repo 直下の `*.md` の PDF 自動再生成**は Codex 側にもある
  （`.codex/hooks.json` の `PostToolUse` → `scripts/md2pdf-hook.sh`）。
  **初回または定義変更後は `/hooks` で内容を確認して trust する**（trust するまで動かない）。
  Claude Code 側は `.claude/settings.json` から同じスクリプトを使う。
- **`.codex/config.toml`** に既定を置いている（`approval_policy` / `sandbox_mode=workspace-write` /
  **`network_access=true`**）。tools の取得（`scripts/fetch_tools.sh`）と、終了後の真値の取得
  （`scripts/fetch_truth.sh`）に外部通信が要るため。
- 一時ファイルはリポジトリを汚さない場所に置く（`out/` `mass/` が既存の出力先。`.gitignore` 済み）。
- それ以外（コマンド・比較の作法・記録先）は `CLAUDE.md` と `knowledge/` のとおりで、Codex でも変わらない。
