# CLAUDE.md — AHC 作業ルール(AIエージェント向け)

このリポジトリは AtCoder Heuristic Contest(AHC)1本。**まず §0 の規約ゲートを通ること。**

## 0. AI 利用規約ゲート(最優先・他の何よりも先に)

AHC の AI 利用規約は急速に変化する。**本コンテストの規約を、他のいかなる行動よりも先に確認する。**
規約は次のインポートで読み込まれる:

@problem/ai_guideline.txt

- **規約が読み込まれていない(上のファイルが空 / 未整備)の場合**:
  「本コンテストの AI 利用規約が未読込です(`problem/ai_guideline.txt` が空)。規約を貼り付けてください。」
  **とだけ出力し、思考・調査・ファイル読み書き・コマンド実行・実装を含む他の一切の挙動を行わず即座に停止する。**
  未読の規約に違反しないための fail-closed(規約の有無を確認する行為だけは例外)。
- **規約が読み込まれている場合**: その規約を**最優先で順守**した上で以下に従う。以下と規約が矛盾すれば規約が優先。

── ここから下は「規約が読み込まれている」前提の作業ルール ──

## 前提

- **棲み分け**: **方法論・作業ルール・過去の失敗例はこのファイル。このコンテストの記録は `LOG.md`**
  (両方に書くと必ず片方が古くなる。ルールを足すならここ、記録を足すなら LOG.md)。
  - 🔴 **割る軸は 2 本**: ①**更新のされ方**(書き換える / 追記する / 不変) ②**読み直す単位**。
    **粒度は「書く単位」ではなく「読み直す単位」に合わせる** ── 分割の目的は書きやすさではなく、
    **前提が変わったときに読み直す範囲が 1 ファイルに収まること**(§5 の再走規律)。
  - **記録が育ったら `docs/` へ移して 3 分割する**(要求が正反対なので同居させない):
    **`LOG.md` = 盤面**(現在地/打ち止め/次の一手/register/否定の棚/env。**短く常に最新に**) ／
    **`EVIDENCE.md` = 証拠**(実験の全記録。**append-only**。一度書いた測定は書き換えない) ／
    **`SETUP.md` = 前提**(問題の要約/構造カルテ/**用語集**/実行方法。ほぼ不変)。
  - 🔴 **証拠がさらに育ったら「族ごとに 1 ファイル」へ割る**(`docs/ev/<族>.md`)。
    **1 つのファイルに「族順」と「append-only」を同時に課してはいけない ── 両立しない。**
    本戦中は追記位置を決める余裕が無いので必ず末尾に積まれる(ahc069: 4 本が族に入りそこねて G と F の間に
    取り残され、見出し `F` が 2 つになった)。**族ごとに割れば「末尾に追記」＝「族順」が自動で一致し、
    追記のたびの判断が消える** ── **本戦中に判断を要求する規律は必ず破られる。**
    ⚠ **1 実験 1 ファイルまでは割らない。** ahc069 で効いたのは「repack の知見を 8 箇所から 1 節に集めた」ことで、
    細かくするとその勝ちを戻す。**族 ＝ 再監査の単位**が粒度の下限。
  - 🔴 **盤面は割らない。代わりに行数上限(目安 150 行)を課して台帳を押し出す。**
    盤面は毎セッション読むので、割ると「現在地を知るのに 5 ファイル開く」になり**逆に悪化する**。
    押し出す先: **版の一覧 → `VERSIONS.md`** / **変更履歴 → git log(または `HISTORY.md`)** / **用語集 → `SETUP.md`**。
    (ahc069: 盤面が 851 行 155KB まで膨らみ、**盤面本体は全体の 12%**、残りは台帳と辞書だった。
    履歴を押し出したのは**最終日**。**用語集を盤面に置いたせいで、凍結時に新ログへコピーされて二重化した**)。
  - 🔴 **住所は「節番号」ではなく「ID」にする。** 否定 `N1..`・候補 `C1..`・版番号を**一度振ったら動かさず**、
    **見出しとコミットメッセージの両方に入れる** ⇒ **`grep -rn C13b docs/` がそのまま索引**になる。
    **手書きの索引表は必ず腐る**(ahc069 の索引は自ら「節番号は分割前のまま」と書いており、欠番が 2 つあった)。
  - ⚠ **`CLAUDE.md` はリポジトリ直下に置く**(ハーネスが `./CLAUDE.md` を自動読込するため移動できない)。
  - **`AGENTS.md`(Codex 等の入口)は「このファイルを読め」と言うだけの薄いシム**。**規約とハーネス差分以外を書かない**
    ── ルールを写した瞬間に二重管理になる。
- **役割分担**: 方針(何を狙うか・どのアイデアを追うか)は**ユーザーが握る**。AI は**計測・実装・否定結果の
  整理**を担当し、**枠組みレベルのリスクを能動的に提起する**(小さな改善の逐次報告より優先し、後回しにしない)。
- 🔴 **ユーザーがコードを読む機会は、AI 側が作る。** 過去 2 回の一番の失敗は**2 回とも実装上の選択**で、
  しかも**問題文と並べて読めば数分で気づく**種類だった(ahc068: 配達を距離最短で実装 / ahc069: fit 条件を
  条文より狭く実装)。**にもかかわらず A/B にも register にも現れなかった ── 全変種に共通なので差分に出ない。**
  **差分に出ないものを捕まえられるのは、差分ではなく本文を読む目だけ。**
  ⇒ **監査を回すたび(§3)に「前提の抜き書き」を出す**:
  ① **可否を決める `if` の全文**(条文の該当箇所を**並記**する) ② **1手の primitive の本体**
  ③ **共通部品に昇格した関数**(全分岐が通る場所)。
  **20〜40 行に収める** ── 読まれない量を出したら意味がない。**読む相手が居ない実装は、書いた本人の思い込みが検査されない。**
- **日本語で応答する。**
- 着手前に `LOG.md`(特に §0 用語集・§1 序盤チェックリスト・§3 設計上の選択 register)を読む。
- **`knowledge/README.md` の索引を見る**。**手法の索引 = 手持ちの一覧**なので、`LOG.md §2` のカルテを
  埋めたら**軸で手法を選ぶ**(評価関数を登る族と、出力列の長さを削る族は**別のゲーム**。混同すると空振りする)。
  参加記録の索引で**型が近い回があればその回を読む**。行き詰まった時は「一番の失敗」列を読み直す。
  **正本は `template/knowledge/`** なので、ここにあるコピーは編集しない ──
  **コンテストが終わったら参加記録・手法・テクニックを書いて template 側に置く**。
- セットアップで人手が要るのは**規約の貼り付けと問題ページの保存の2つだけ**(手順は README)。
  `tools/` が無ければ `scripts/fetch_tools.sh` を案内する(DL/展開/build/入力生成まで自動)。

## コード / アプローチ

- **提出は単一 `.rs`**(外部クレート不可)。再利用部品は `lib/` に**種類ごと1ファイル**で分割保持(各 `mod` ブロック,
  `lib/` が正)。`lib/` は cargo の lib ターゲット `ahc_lib`(`lib/lib.rs` が include!)なので **`cargo check`/`cargo test`
  で通常どおり型検査・テスト**できる。解答は lib を `use` せず**部品をコピペ**する(自己完結でそのまま提出でき,
  その場で改造もできる。cargo-equip 等の bundler は使わない)。
- **1アプローチ = 1物理ファイル** `src/bin/NN_<name>.rs`。**過去の版を絶対に上書きしない**(動く版を失わない)。
- **こまめにコミット**し、メッセージにローカル合計スコアと**関係する ID(`N27` / `C13b` / 版番号)を書く**(§5)。ベスト版を常に復元可能に保つ(実質 main 一本で良い)。
- **ブランチは任意**(必須ではない)。AHC は直列・加算的なので分岐戦略は効きにくい ── 復元性はファイル分割＋
  スコア入りコミットが担保する。切るのは「捨てる前提の大改造で WIP を main に残したくない時」だけ。
- チューナブルは **env**(`AHC_TL` 等)で外出し。再コンパイル無しに掃引できるように。
  **新しいレバーの既定は OFF** にする ── そのまま「OFF で前版と byte 一致」の検証に使えるので、
  機構を足すコストが下がる(下の検証節)。
- **ローカルは遅い前提**。時間予算は上限近くに(自己制限が速いジャッジへ自動追従する)。

## 進め方(測定優先・構造から考える)

> 手順そのものは `LOG.md §1` のチェックリスト。ここは**各項目の根拠**(なぜ省略してはいけないか)。
> **出発点は `LOG.md §2` の構造カルテ8軸**(軸3 だけ 3a 威力 / 3b **可否の前提** の 2 段。定義は `knowledge/README.md`)。
> **各軸に「自分がした実装上の選択」を紐づけ、`LOG.md §3` の register に落とす** ── 過去 2 回の一番の失敗は**2 回ともカルテではなく実装の側**にあった。
> **軸1〜3で解法クラスがほぼ決まるので、埋める前に探索の設計を始めない。**
> 小手先のパラメータ調整でなく、学びのある本質的な探求を。

- **制約の条文から「1手の最大威力」を確認**(実行可能解を書く*前*に)。**パラメータの端**(幅1 / 最小サイズ /
  上限いっぱい / degenerate な形)が合法かを条文で1つずつ確認し、**自然に思える操作ではなく制約が許す
  最も強い操作**を primitive にする ── ahc068 は「矩形の幅は 1 でよい」の一点で 1手の威力が **4倍**違い、
  気づかないまま積んだ全レバーが無駄になった。
- 🔴 **同じ照合を「前提」の側にも掛ける ── 条文が要求する*最も弱い条件*を可否の判定式にする。**
  実装は条文より**狭い**方へ簡単にズレる(都合のよい必要条件を足してしまう) ⇒ **可否の `if` を条文と 1 対 1 で突き合わせる。**
  ahc069 は条文が「**P セルが空きなら置ける**」なのに実装が「外接矩形 w×h が**全部**空き」を要求しており、
  **初日の版から 124 版中 123 版**そのまま。緩めた瞬間に **+1.19%**(単発 2 位の大きさ)。
  ⇒ **「最も強い操作」と「最も弱い前提」は同じ照合の表裏**で、2 回とも同じ型で落とした。
- **解法クラスの天井をスコアの単位で計算する**。1手で進める量から理論下限を出し、スコア式に通す。
  **単位はスコア式に直接入る量で揃える**(距離など代理量で出すと嘘の下限になる)。
  **上位スコアが自分の天井を超えていたら、磨いても届かない ⇒ そう報告してクラスの乗り換えを提案する。**
  (ahc068: `floor=Σceil(|Δ|/10)` ⇒ 2.345手/枚 ⇒「1手1枚」クラスの天井 est 1011M に対し、上位は
  1.87手/枚=1059M で**天井を割っていた** ⇒ 1手で複数枚を進める機構が数学的に必須と確定した。)
  **理論下限(クラスの天井)と実測上限(そのレバーの伸びしろ。時間N倍などで安く測る)は別物**で、
  両方あると「このクラスで磨くか/乗り換えるか」の投資判断が一意に決まる。
- **大きな実装の前に、そのレバーの上限を安く測る**(時間 N 倍 / 自由度を数える / 上界を計算する)。
  - **上界は「厳密な必要条件」で作ると実装の巧拙に依らない**(ahc069: AM-GM で外接矩形の箱を導き、
    上限 1.56% と判明して**実装ゼロで棄却**)。
  - 🔴 **天井は「その時点の版」に対する量。機構を入れたら天井も動く** ⇒ **未実装のまま温めている候補も、
    別の機構を入れたら測り直す**(ahc069: 「ビームの天井 +2.003%」を頭に持ち続け、機構追加後に再測定せず提案 ──
    実際は **+0.278%** で 86% が回収済みだった)。
  - 🔴 **「seed ごとに最適化した答え」と「特徴から予測できる分」は別物。必ず両方測る**
    (ahc069: オラクル **+1.58%** に対し、静的な入力特徴から取れるのは **+0.13%(8%)**)
    ⇒ **予測器を作る前に「予測可能な割合」を測る。** 残りが大きいなら情報は**オンラインの観測量**にある。
  - 🔴 **上界を作ったら「この上界が無視している前提」を列挙する**(ahc069: 移動費しか数えていない天井 +46.3% に対し、
    実際の律速は「どかした相手を再配置できるか」で実現値は +0.3%)。
- **「同じ族だから同じ効果量」と見積もらない。効くのは評価関数が拾える粒度の自由度だけ**
  (ahc069: 同じ「周長不変の自由度」で粗い変更は +0.16%、細かい変更は有意差なし)。
- **同じ評価関数が「経路の片方にしか掛かっていない」箇所を探す**(惰性の典型。安くて効果が読め、ahc069 で **5 連勝**)。
  - 🔴 **適用限界: `A` を最適化するのに使った評価は、`A` の選択後の残差には情報が残らない。**
    効いた 5 本は全部「**まだ誰も最適化していない自由度**」に評価を掛けたもの。
- **裾(worst-case の時間)は本番と同じ件数で直接測る**(ahc069: 静的な入力特徴とは |r|<0.08 で無相関、
  動的な量と r=+0.888。100 seed の max で語ると裾を見誤る)。
- **🔴 定期的に「リファクタ / 計算量 / バグ監査」を回す**(レバーを 3〜5 本試したら 1 回、または大きな実装の前)。
  **「もう手が無い」と感じたときの第一の行き先**(ahc069 では単発最大の一手がここから出た)。
  - **リファクタ**: 1 関数が 300 行を超えたら分割(ahc069 は分割の過程で**提出済みの版のバグ**を発見)。
    ⚠ **必ず別ファイルに写して元版と byte 一致を照合する**(下の検証節)。
  - **監査の優先順位**: ①**効果が出なかった項** ②**コピペで増やした処理** ③**コンパイラの警告**
    ④🔴 **可否の判定式を条文と突き合わせる**(上の「最も弱い前提」)。
  - 🔴 **成果物は「前提の抜き書き」をユーザーに出すこと**(§1)。**監査を回すのは AI でよいが、
    前提が条文と合っているかを判定できるのは条文を読んだ人間**なので、**抜き書きを出すまでが監査**。
  - **数字をコードと `LOG.md` の 2 箇所に書かない** ⇒ **コードには意味だけ書き、数字は `LOG.md` を参照する。**

### 速度・計算量を攻めるとき (ahc069: 10 段で −51.4% ／ 空振り 2 本)

- **推測で削らず全数監査する**(`lib/prof.rs` を貼って `ON=true`)。**回数と1回あたりを分けて出す**まで
  削り所は見えない(ahc068: 同じホットループ内の O(N) 19.7M回 と O(1) 25.3M回。間引くべきは前者だった)。
  **回数が多い ≠ 高い**(ahc069: 本命は推測と別の段で 56〜85%)。
- **🔴 段の割合が分かっても「その段の中で何が高いか」は別問題。「同じデータを何度も走査している」は削る理由にならない。**
  ahc069 で 2 連続で外した: ① 狙った項は段の 17% のうち 7% ② L1 に乗る 2500B を 3 走査 → 1 本に融合したら
  **+0.9% で悪化**(コストは読み込みでなく書き込みと計算。**融合は依存を増やしてベクトル化を阻害する**)。
  ⇒ **段の中をもう一段割って測る。**
- **🔴 「f64 の比較だから整数化できない」と机上で決めない。状態空間が小さければ全数検証できる**
  (ahc069: 評価を整数キー 1 本に畳んで **単発 −19.8%**)⇒ **総当たりを起動時に回すだけで可否が確定する。**
  食い違う値では panic させる([`knowledge/techniques/equivalence-by-assert.md`](knowledge/techniques/equivalence-by-assert.md))。
- **掃引の plateau(同一結果の平坦域)からは「実装が有利な値」を選べる**
  (ahc069: 既定を 0.2 → 0.25 にずらすだけで byte 一致を保ったまま整数化できた)。
- **内側ループの「1 要素あたり ns」まで測れたら、byte 一致で削れる余地は尽きたと判断してよい**
  (そこから先は呼び出し回数を減らす話で、**それはスコアに影響するレバー**)。
- **配分と計算量を混同しない**(ahc069: 配分では +0.28% が上限 → 計算量を直したら +0.70%)。
  **高速化そのものの Δ は 0.000%(byte 一致)で、価値は「空いた予算」にしか現れない** ⇒ **レバーとセットで評価する。**
- **🔴 壁時計で打ち切っている段が残っていると、高速化は「予算」にならない**(下流がその締切まで多く働くだけ)。
  **作業量カウンタで打ち切る段でだけ高速化が予算になる**(ahc069: 素の時間 −8.79% を作っても、
  ローカルのスコアも裾も動かなかった。⚠ ただし**ジャッジ側では裾に出た** ── 自分の seed 集合に重い例が無かっただけ)。
- **🔴 空いた予算の再配分は「使い先の機構」が決まってから行う**(機構を入れると最適点が動く)。

## 検証と計測の作法(省略しない)

> ここを飛ばすと「何が効いたのか」が永久に分からなくなる。速く進むためではなく**原因を特定できる状態を
> 保つため**のルール。

- **新機構は「無効化した設定で前版と byte 一致」を確認してから有効化する**(`AHC_NEW=0` 等)。
  `A_ENV="AHC_NEW=0" scripts/same.sh <新> <前版> 20` が `**完全一致**` を出せば、以後の差分は
  新機構だけに帰属できる ⇒ 大きな書き換えを安全に進められる。一致しないまま進めると原因が特定できない。
- 🔴 **byte 一致は「機構を足す」ための道具であって「核を書き換える」ための道具ではない。**
  通すには**既存コードを触らず分岐を足す**しかないので、**初日に書いた核が全分岐の共通項として凍る**
  (ahc069 の fit 条件は 124 版中 123 版で不変。共通項なのでどの A/B にも写らない)。
  ⇒ **核を書き換えるときは同値検査を assert に切り替える**: 旧実装を残し、**実走行中に同じ入力で両方を評価して
  `assert!(旧 == 新)`**。通ってから旧を捨てて拡張する。byte 比較は「違う」しか教えないが、
  assert は**どの入力でどう違うか**を教える(ahc069 では実際に食い違いを検出し、それが発見そのものになった)。
  → [`knowledge/techniques/equivalence-by-assert.md`](knowledge/techniques/equivalence-by-assert.md)
- **🔴 リファクタ・高速化は元版を書き換えず、別ファイル(`NN+1_*.rs`)に写してから行う**
  (元版を直接いじると比較の基準そのものが消える)。**提出した版のファイルは以後 read-only 扱い。**
- **🔴 速度だけを変える最適化の byte 一致検証は、壁時計ゲートを無効化して行う**(`AHC_TL` を巨大にする)。
  **速くなった版は、壁時計が絡む限り必ず出力が変わる**(ahc069: `same.sh` 9/20 の原因はバグではなくこれ)。
- **計測用の版も同じ扱い**。診断カウンタは別ファイル(`NNd_diag.rs`)に入れ、**前版と byte 一致**を確認する。
- **出力そのものを1手ずつ検査する**(`AHC_VERIFY=1`。`00_base.rs` の検証節を参照)。
  **終状態の検査では足りない**: 内部の適用関数が制約を無視していると、内部盤面は完成しているのに
  提出は失格になる(ahc068 で実際に踏んだ。「解けている」と「合法である」は別)。
- **壁時計は最外の締切だけに使う**。内部の重い段を壁時計で打ち切ると**解が非決定的**になり
  (同一バイナリ・同一入力で出力が変わる)、掃引の比較も byte 一致検証も壊れる。
  内部段は `timer::Budget`(作業量カウンタ)で打ち切り、単位→ms の換算は1度キャリブレーションする。
  **並列で掃引するなら決定性は必須。**
### 比較の作法

- **比較は同一 seed 集合・同一 env・同一時間予算でのみ行う**(`results.meta` に両方記録されるので必ず照合)。
- **🔴 実 TL の直列計測は並列ジョブと同居させない。計測前に `/proc/loadavg` を見る。**
  ⚠ **背景ジョブの「完了通知」＝ 負荷ゼロではない**(5 分平均はまだ高い)。**負荷が閾値を下回るまで待つループを
  計測スクリプトの前に置く**(ahc069 で 2 回踏んだ。裏で 11 並列が走ったまま測って「−1.26% で悪い」と誤判定し、
  単独で測り直すと **+0.38%** で符号が逆だった)。
- **🔴 計測の並列度そのものが採否を変える。** 並列数を上げると per-case の実時間が膨らみ、**壁時計の弁を
  人工的に発火させる**ので、**時間を食うレバーだけが不当に負ける**
  (ahc069: `nproc−1` 並列は `Δlog` を **−0.740%** 目減りさせた。**`JOBS=3` なら真値を 0.06% 差で再現**した)。
  ⇒ **時間中立なレバーは並列で測ってよいが、時間を食うレバーは低並列で測り直す。**
  **自分のパラメータで最適化済みの変種を、最適化していないベースラインと比べない**
  (不公平な比較は偽の否定/偽の勝ちを作り、それが記録に混ざると後の判断まで汚染される)。
- **レバーの併用は net で測る**(単独の +X を足し算しない。時間予算を食い合うので、組み合わせで掃引し直す)。
- **「小さいレバー」だと自分の見積もりだけで判定しない**(ahc068: root 本数を「プロセス全体の壁時計 ÷ 1本」で
  概算し実測の 1/3 以下に見誤った。**カウンタを入れてから語る**)。

#### 時間の扱い (ahc069 で 3 回続けて判断を誤った領域)

- **🔴 時間予算を外した掃引(screening)は「決定性のため」であって採否には使えない。時間を食うレバーは
  必ず実 TL・同一セッション・paired で測り直す。** ahc069 では**符号が反転した実例が 4 本**:
  掃引 +0.455% が実 TL で実行不能 ／ +0.724% → **−0.315%** ／ +0.437% → **−6.563%** ／ +0.192% → **−0.269%**。
  原因はいずれも**安全弁の発火**で、発火すると以後の出力が捨て値になる。⇒ **採否の表には「弁の発火率」を併記する。**
- **新機構の時間コストは「平均」ではなく「裾」で評価する**(重い入力ほど新機構も多く発火するので裾に集中して乗る)。
- **内部段の打ち切りは作業量カウンタが一次制御、壁時計は安全網。**
- **時間(ms)の版間比較も同一セッションで paired に測る**(ahc069: 別セッションの比較で「速くなった」と誤判定した)。
- **🔴 ローカル↔ジャッジの時間校正は「仕事の種類」が変わると係数が動く。新機構を入れたら取り直す**
  (ahc069: compute 律速 0.853 → 別機構の追加後 1.068)。⚠ **比を取る前に弁が出ていないか確認する。**
- **ローカル ms とジャッジ ms は「比」ではなく `加算オーバーヘッド + 係数` で結ぶ**(tester の分は加算)。
  **1点で比を作って外挿すると壊れる。2点以上で最小二乗**なら ±3% で合う。
- **🔴 予算を増やすなら「基数」ではなく「適応の上限」を上げる。** 基数を上げると裾のターンで弁を突き抜ける
  (ahc069: 箱予算の基数 2.5 倍で **−0.74%**、適応上限だけ 2 倍なら **+0.075%**)。

#### 順位表から読める量の扱い

- **順位表由来の量の差分は、日単位の時間差がある提出の間でしか読めない。**
  **提出のたびに「日付」ではなく「時刻」を記録する**(JST)。
- **🔴 順位表のペースを「%/日」で持ってはいけない**(投入時間は**曜日**で変わり、伸びも**序盤ほど大きく後半は飽和する**)。
  ⇒ **コンテスト期間の曜日を最初に書き出し、残りを「平日 n 日 ＋ 週末 m 日 ＋ 最終日」で数える**(最終日は上振れ)。
  ⚠ 「週末か」と「序盤か」は交絡する。分離できないときは点推定を出さず**幅で持ち、投資判断は幅の下限で決める**。
- **★ 相対評価では「順位表を読むだけ」で分母の時間発展が測れる**(自分の相対は他人が提出するたびに再計算される)
  ⇒ **提出を消費せずに何度でも観測できる**。同じ時刻に読めば区間が揃う。
  **大きな構造変更は週末の前に入れ切る**(この判断は幅に依存しないので迷わず実行してよい)。
- **🔴 `envelope`(per-case max)は「自分の順位」を測る道具ではない。順位を決めるのは自分の周りの密度**
  (ahc069: envelope も自分の相対もほぼ不変なのに 11位 下降)。**順位の密度(`1% ≒ N位`)を実測すれば、
  周辺のペースを順位の変化から逆算できる** ⇒ **投資判断には周辺のペースを使う。**
  `envelope` の用途は **①クラス乗り換えの要否 ②到達不能分の分離** の 2 つに限る。
- **`envelope` は単調増加ではない**(分母は各人の**最良**ではなく**最新**提出の max)。小さな減少をノイズと決めつけない。

### 相対評価のコンテストでの判定

**各ケース `1e9 × 自分/全参加者最大` の和で順位が決まる形式では、絶対スコア和で判定してはいけない**
(seed ごとの絶対スコアは入力で何倍も違うのに順位表ではどの seed も 1 枠。ahc069 は seed 間で 8.8 倍)。

- **判定は2つの統計を並べ、両方が有意に正のときだけ採用する**:
  1. **`Δrel`(算術)** = `mean_i((a_i−b_i)/D_i)`。真の目的関数と同じ形だが**分母 `D_i` の質に依存する**。
  2. **`Δlog`(対数平均)** = `mean_i ln(a_i/b_i)`。**分母が完全に消える**ので、**分母を選ばずに済む唯一の統計**。
  - **符号が食い違ったら採用しない**(群別に分解してから判断する)。
- **採否は paired 比較**(同一 seed の差)で決め、**`> 2se` を門にする**。
- **必要な seed 数は `2se ≦ 検出したい改善幅` から逆算する**(`se ≈ σ/√n`)。**件数はコストではない**(制約は掃引の壁時計だけ)。
- **dev / holdout を分け、最終判断は未使用の holdout で確認する**
  (同じ集合で k 個から最良を選ぶと勝者は水増しされる。ahc069: k=5 で 1.16se、k=20 で 1.86se)。
- **固定の参照点を1つ作る**(自分たちのベストを分母にすると**動く分母**になり、セッション間で進捗を追えない)。
- **🔴 提出画面は判定に使えない。採否はローカルの paired 統計だけで決め、提出は校正専用にする。**
  ノイズは `sd(rel(n))/rel = CV(r)/√n`(ahc069 の 50 ケースでは sd ≒ 3% ⇒ **1% 未満のレバーは提出では原理的に見えない**)。
- **🔴 「暫定と最終の乖離」は向きまで予測しない**(ahc069 で外した)。**暫定の偏りは「採否に使わない理由」であって、
  「最終の上振れを見込む理由」ではない。**

#### 分母(`envelope` = per-case max)を逆算するときの規律

- **逆算した分母の偏りは閉じた形で書ける(厳密)**: `Ê = D̄ + cov(D,r)/r̄` ⇒ **相対偏り = `ρ(D,r)·CV(D)·CV(r)`**。
  **一様な改善では `Ê` は不変** ⇒ 人工物は**固定参照点に固定した対照**で測れる。
  **`CV` が大きい問題ではこの逆算は壊れる**(ahc030 の真値で中位層は −72% 外れた。ahc069 は ±0.12%)。**先に `CV` を見る。**
- **`envelope` との差を「伸びしろの一覧」と読んではいけない。到達不能分を分離する。**
  **首位の相対スコアを読めば分離できる**(`首位の rel/満点` = 首位が取る分母の割合)。
  **回によって正反対**(ahc030 は 71.4% ＝ 差の大半は誰にも埋まらない ／ ahc069 は 99.35% ＝ 差はほぼ全部実在)⇒ **毎回読む。**
- **検証は過去回の真値でできる**: `img.atcoder.jp/ahc_standings/<contest>/{input.csv,result.csv}` が**ログイン不要**。
  `result.csv` は**全参加者 × 全 seed のスコア行列**。**自分の推定式を真値に当てて検算してから使う。**
- 🔴 **コンテストが終わったら推定は要らない。順位を直接測れる。** `input.csv` の seed で**本番と同一のケースを
  再生成**し、`result.csv` を分母にすれば「この版は何位相当か」が毎回出る(ahc069 の延長戦はこれ一本で判定した。
  ⚠ **公式 `gen` は seeds ファイルの*行番号*でファイル名を決める** ⇒ 下の事故に注意)。

### 長い測定は「いつ終わるか」を先に出す

- **🔴 見込み 2 分を超える測定・掃引・ビルドは、走らせる前に「予測所要時間」と「予測終了時刻」を出す。**
  形式: `n=1200 / JOBS=11 ⇒ 予測 18 分・終了 23:41 JST`(根拠を1行併記)。
- **根拠は実測から作る**(`n × per-seed ÷ 並列数 ÷ 効率`)。分からなければ **20 seed のパイロットを先に回す**。
  掃引は **`設定数 × 1 設定あたり`** に分解する。
- **長いものは背景実行にし、待つ間に自分が何をするかも書く。予測が 2 倍以上外れたら、その場で報告して基準を更新する。**
- ⚠ **背景の並列掃引と直列計測を同居させない** ⇒ **順番と各予測終了時刻をまとめて宣言してから走らせる。**

### 道具

| 用途 | コマンド |
|---|---|
| 時間の計測(**直列**なので per-seed ms が信用できる) | `scripts/test.sh <bin> [num] [KEY=val]` |
| 採否の判定(**並列**・大量 seed。ms は測らない) | `scripts/mass.sh <bin> [num\|from:to] [KEY=val]` |
| 相対比較(`Δrel` / `Δlog` / 必要 n / 群別分解) | `python3 scripts/rel.py mass/A.csv mass/B.csv [--seeds a:b] [--group <col>] [--ref <csv>]` |
| byte 一致照合 | `A_ENV="AHC_X=0" scripts/same.sh <新> <前> [num]` |
| 赤字の構造 | `python3 scripts/measure.py` / 入力特徴量 `scripts/feat.py` / 固定参照点 `scripts/ub.py` |
| プロファイル | `lib/prof.rs` を貼って `ON=true` |
| **過去回・終了後の真値**(分母/ノイズの検証、順位の直接計算) | `curl https://img.atcoder.jp/ahc_standings/<id>/result.csv`(全参加者×全 seed) ／ 入力特徴量は `input.csv` |
| **過去回の問題文 / 解説** | `atcoder.jp/contests/<id>/tasks/<id>_a` ／ `.../editorial`(**上位の参加記が順位つきで並ぶ**) ／ 公式スライド `img.atcoder.jp/<id>/<id>.pdf` |

- 🔴 **公式 `gen` は「seeds ファイルの*行番号*」でファイル名を決める**(seed 値ではない)。
  `seq 10000 11199 > s.txt; gen s.txt` は **`in/0000.txt`〜`in/1199.txt` を破壊する**(ahc069 で実際に踏み、
  走行中の掃引ごと汚染した)⇒ **追加生成は必ず `--dir <別ディレクトリ>` に出す。**
  復旧は生成器が決定的なので同じ seed を作り直せばよいが、**上書きされていない既存ファイルと byte 一致を照合してから**宣言する。
- 上はすべて**ログイン不要**(`ahc_standings` は終了後の公開なので開催中の回は 403)。
  **順位表(`/standings/json`)と開催中の問題ページはログイン壁** ⇒ **首位の相対スコアなどは人に読んでもらう。**
- **WebSearch は最新 2〜3 回に届かない** ⇒ **`editorial` ページから辿る。**

## 記録の規律(`LOG.md` を嘘にしない)

- **🔴 記録する時刻はすべて JST で書く**(`2026-08-07 22:41 JST` の形)。
  **「残り n 日」のような相対量は、書くたびに絶対時刻から計算し直す**(放置すると静かに嘘になる)。
- **`LOG.md §3` の「設計上の選択」register を維持する**。実装の選択を**問題の性質として書かない**
  (共通部品に昇格した選択は**問題の性質の語調で書かれる**ため、どの A/B 比較にも写らず計測規律を
  すり抜け続ける)。**レバーが連続して負に出たら「近最適に収束した」と読まず、`変えた実績` が空の行を
  上流から壊す** ── **レバー全滅は収束の証拠ではなく、共通の上流前提を疑う合図**
  (ahc068 は配達 primitive がこれで、5つのレバーが全滅したのを「収束」と読んだが、実際は全部が
  1つの未検査の選択の下流だった)。
  🔴 **可否の判定式と 1手の primitive は、書いた/変えたその場で register に 1 行足す**(監査まで待たない)。
  **待つと「共通部品なので誰も*選択*だと思っていない」状態が固定する** ── これが 2 回とも起きた形。
  **🔴 1つの行が「✅ 変えた」になると、同じ行に混ざっていた別の惰性が見えなくなる** ⇒ **変えた時に行を割る**
  (ahc069: 「候補生成」を形族と位置に割ったら +9.6%)。**register に持つ行は評価関数や候補生成だけでなく、
  「可否の判定式」そのものも含める。**

#### 「効かなかった」を確定させる前の 4 段 (ahc069 で全部踏んだ)

1. **出力が変わったか確認する。** 変わっていないなら実装が無効化されている。変わっているのに net が 0 なら
   **勝ちと負けを同数生んでいる** ⇒ **勝敗の件数を必ず併記する。**
2. **項の値を 1 度は印字して目視する**(ahc069 は判定が常に true ＝ 別の量を数えていた)。
3. **ただし「壊れている ＝ 直せば伸びる」ではない** ⇒ **バグ修正も 1 つのレバーとして paired で測ってから採否を決める。**
4. **同じ選択の基準を 3 通り試して全部 net ≒ 0 なら、「基準」ではなく「選択肢の数」を疑う**
   (ahc069 の律速は**候補の 18.3% しか成立しない**ことだった)。
- **否定的結果は「前提つき」で `LOG.md §6a` の棚に記録する**(「A は効かない」ではなく「**B が無い状態では**
  A は効かない」)。🔴 **棚に載せるとき `N1, N2, …` の ID を振る**(候補は `C1, C2, …`)。
  **ID は一度振ったら動かさない**(枝分かれは `C5b`)。**再走の指示・コミットメッセージ・証拠の見出しを
  この ID で結ぶ ⇒ `grep -rn N27 .` が索引になる**(§1。手書きの索引表は分割のたびに腐る)。**バグ修正・機構追加・§3 の選択変更のあとは、前提が変わった否定を再走させる**
  (否定は永久ではない。ahc068 では否定済み実験の1本の再走が**単一最大の +49M**。記録していても
  再監査しなければ死蔵になる)。「機構が悪い」と「自分の実装が悪い」を切り分けずに否定として確定しない
  (ahc068: 却下率 85% の原因は機構ではなく制約の張り忘れだった)。
- **枠組みが反証されたら、`LOG.md` を凍結して `LOG2.md` を新設する**(`cp LOG.md LOG2.md` してから書き替える)。
  **凍結した旧ログの冒頭に「以後の前提として再利用してはいけない結論」の表を置く**
  (否定された結論が新しい推論に紛れ込むのを防ぐ)。
  🔴 **凍結時に「コピーして引き継ぐ」ものが出たら、それは盤面ではなく前提**(用語集がこれ)
  ⇒ **コピーせず `SETUP.md` へ移して両方から参照する**(コピーした瞬間に二重管理になる。§1)。
  **凍結する必要があるのは「その時点の判断」だけで、定義や条文は凍結の対象ではない。**
- **順位表から読んだ数字は「その時点のスナップショット」として記録する**(上位は伸びるので目標値として固定しない)。
  **投資先は自分の赤字の構造で決める。**
- **コミットのたびに `LOG.md §8 変更履歴` に1行追記。**

## 知見の追記(コツ・解法)

- 他人発の知見は**自分の言葉で言い換え＋出典リンク**で載せる。アイデア/技法は著作権対象外だが、
  他者の**文章・図・コード片の逐語コピーは避ける**(本テンプレは CC0。第三者素材は再ライセンスできない)。
- やむを得ず引用する時は短く・出典明記・「CC0対象外」と注記する。

## git

- **`git add` は関係ファイルのみ**(無関係な変更を巻き込まない)。破壊的/外向きの操作(push 等)は指示があるまでしない。
- **この contest repo は private 前提**。`problem/` に AtCoder の問題文(著作物)を追跡するため public 化しない。
  public への push/公開を求められたら、再配布になる旨を指摘して確認を取る(テンプレ `ahc-template` のみ public)。
