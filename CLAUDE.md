# CLAUDE.md — AHC 作業ルール(AIエージェント向け)

このリポジトリは AtCoder Heuristic Contest(AHC)1本。**まず §0 の規約ゲートを通ること。**

## 0. AI 利用規約ゲート(最優先・他の何よりも先に)

AHC の AI 利用規約は急速に変化する。**本コンテストの規約を、他のいかなる行動よりも先に確認する。**
規約は次のインポートで読み込まれる:

@problem/ai_guideline.txt

- **規約が読み込まれていない(上のファイルが空 / 未整備)の場合**:
  「本コンテストの AI 利用規約が未読込です(`problem/ai_guideline.txt` が空)。規約を貼り付けてください。」
  **とだけ出力し、思考・調査・ファイル読み書き・コマンド実行・実装を含む他の一切の挙動を行わず即座に停止する。**
  未読の規約に違反しないための fail-closed(規約の有無を確認する行為だけは例外)。
- **規約が読み込まれている場合**: その規約を**最優先で順守**した上で以下に従う。以下と規約が矛盾すれば規約が優先。

── ここから下は「規約が読み込まれている」前提の作業ルール ──

## 前提

- **棲み分け**: **方法論・作業ルール・過去の失敗例はこのファイル。このコンテストの記録は `LOG.md`**
  (両方に書くと必ず片方が古くなる。ルールを足すならここ、記録を足すなら LOG.md)。
- **役割分担**: 方針(何を狙うか・どのアイデアを追うか)は**ユーザーが握る**。AI は**計測・実装・否定結果の
  整理**を担当し、**枠組みレベルのリスクを能動的に提起する**(小さな改善の逐次報告より優先し、後回しにしない)。
- **日本語で応答する。**
- 着手前に `LOG.md`(特に §0 用語集・§1 序盤チェックリスト・§3 設計上の選択 register)を読む。
- **`knowledge/README.md` の索引を見る**(過去コンテストの分析。**問題の型が近い回があればその回のファイルを読む**)。
  行き詰まった時は索引の「一番の失敗」列を読み直す。**正本は `template/knowledge/`** なので、
  ここにあるコピーは編集しない ── **コンテストが終わったら分析を書いて template 側に置く**。
- セットアップで人手が要るのは**規約の貼り付けと問題ページの保存の2つだけ**(手順は README)。
  `tools/` が無ければ `scripts/fetch_tools.sh` を案内する(DL/展開/build/入力生成まで自動)。

## コード / アプローチ

- **提出は単一 `.rs`**(外部クレート不可)。再利用部品は `lib/` に**種類ごと1ファイル**で分割保持(各 `mod` ブロック,
  `lib/` が正)。`lib/` は cargo の lib ターゲット `ahc_lib`(`lib/lib.rs` が include!)なので **`cargo check`/`cargo test`
  で通常どおり型検査・テスト**できる。解答は lib を `use` せず**部品をコピペ**する(自己完結でそのまま提出でき,
  その場で改造もできる。cargo-equip 等の bundler は使わない)。
- **1アプローチ = 1物理ファイル** `src/bin/NN_<name>.rs`。**過去の版を絶対に上書きしない**(動く版を失わない)。
- **こまめにコミット**し、メッセージにローカル合計スコアを記録。ベスト版を常に復元可能に保つ(実質 main 一本で良い)。
- **ブランチは任意**(必須ではない)。AHC は直列・加算的なので分岐戦略は効きにくい ── 復元性はファイル分割＋
  スコア入りコミットが担保する。切るのは「捨てる前提の大改造で WIP を main に残したくない時」だけ。
- チューナブルは **env**(`AHC_TL` 等)で外出し。再コンパイル無しに掃引できるように。
- **ローカルは遅い前提**。時間予算は上限近くに(自己制限が速いジャッジへ自動追従する)。

## 進め方(測定優先・構造から考える)

> 手順そのものは `LOG.md §1` のチェックリスト。ここは**各項目の根拠**(なぜ省略してはいけないか)。
> **出発点は `LOG.md §2` の構造カルテ8軸**(各軸の定義と「何が決まるか」は `knowledge/README.md`)。
> **軸1〜3で解法クラスがほぼ決まるので、埋める前に探索の設計を始めない。**
> 小手先のパラメータ調整でなく、学びのある本質的な探求を。

- **制約の条文から「1手の最大威力」を確認**(実行可能解を書く*前*に)。**パラメータの端**(幅1 / 最小サイズ /
  上限いっぱい / degenerate な形)が合法かを条文で1つずつ確認し、**自然に思える操作ではなく制約が許す
  最も強い操作**を primitive にする ── ahc068 は「矩形の幅は 1 でよい」の一点で 1手の威力が **4倍**違い、
  気づかないまま積んだ全レバーが無駄になった。
- **解法クラスの天井をスコアの単位で計算する**。1手で進める量から理論下限を出し、スコア式に通す。
  **単位はスコア式に直接入る量で揃える**(距離など代理量で出すと嘘の下限になる)。
  **上位スコアが自分の天井を超えていたら、磨いても届かない ⇒ そう報告してクラスの乗り換えを提案する。**
  (ahc068: `floor=Σceil(|Δ|/10)` ⇒ 2.345手/枚 ⇒「1手1枚」クラスの天井 est 1011M に対し、上位は
  1.87手/枚=1059M で**天井を割っていた** ⇒ 1手で複数枚を進める機構が数学的に必須と確定した。)
  **理論下限(クラスの天井)と実測上限(そのレバーの伸びしろ。時間N倍などで安く測る)は別物**で、
  両方あると「このクラスで磨くか/乗り換えるか」の投資判断が一意に決まる。
- **速度で詰まったら、推測で削らず先に全数監査する**(`lib/prof.rs` を貼って `ON=true`)。
  **回数と1回あたりのコストを分けて出す**まで、どこを削るべきかは見えない
  (ahc068: 同じホットループ内の O(N) 19.7M回 と O(1) 25.3M回。間引くべきは前者だった)。

## 検証と計測の作法(省略しない)

> ここを飛ばすと「何が効いたのか」が永久に分からなくなる。速く進むためではなく**原因を特定できる状態を
> 保つため**のルール。

- **新機構は「無効化した設定で前版と byte 一致」を確認してから有効化する**(`AHC_NEW=0` 等)。
  `A_ENV="AHC_NEW=0" scripts/same.sh <新> <前版> 20` が `**完全一致**` を出せば、以後の差分は
  新機構だけに帰属できる ⇒ 大きな書き換えを安全に進められる。一致しないまま進めると原因が特定できない。
- **出力そのものを1手ずつ検査する**(`AHC_VERIFY=1`。`00_base.rs` の検証節を参照)。
  **終状態の検査では足りない**: 内部の適用関数が制約を無視していると、内部盤面は完成しているのに
  提出は失格になる(ahc068 で実際に踏んだ。「解けている」と「合法である」は別)。
- **壁時計は最外の締切だけに使う**。内部の重い段を壁時計で打ち切ると**解が非決定的**になり
  (同一バイナリ・同一入力で出力が変わる)、掃引の比較も byte 一致検証も壊れる。
  内部段は `timer::Budget`(作業量カウンタ)で打ち切り、単位→ms の換算は1度キャリブレーションする。
- **比較は同一 seed 集合・同一 env・同一時間予算でのみ行う**(`results.meta` に両方記録されるので必ず照合)。
  **自分のパラメータで最適化済みの変種を、最適化していないベースラインと比べない**
  (不公平な比較は偽の否定/偽の勝ちを作り、それが記録に混ざると後の判断まで汚染される)。
- **レバーの併用は net で測る**(単独の +X を足し算しない。時間予算を食い合うので、組み合わせで掃引し直す)。
- **「小さいレバー」だと自分の見積もりだけで判定しない**(ahc068: root 本数を「プロセス全体の壁時計 ÷ 1本」で
  概算し実測の 1/3 以下に見誤った。**カウンタを入れてから語る**)。

## 記録の規律(`LOG.md` を嘘にしない)

- **`LOG.md §3` の「設計上の選択」register を維持する**。実装の選択を**問題の性質として書かない**
  (共通部品に昇格した選択は**問題の性質の語調で書かれる**ため、どの A/B 比較にも写らず計測規律を
  すり抜け続ける)。**レバーが連続して負に出たら「近最適に収束した」と読まず、`変えた実績` が空の行を
  上流から壊す** ── **レバー全滅は収束の証拠ではなく、共通の上流前提を疑う合図**
  (ahc068 は配達 primitive がこれで、5つのレバーが全滅したのを「収束」と読んだが、実際は全部が
  1つの未検査の選択の下流だった)。
- **否定的結果は「前提つき」で `LOG.md §6a` の棚に記録する**(「A は効かない」ではなく「**B が無い状態では**
  A は効かない」)。**バグ修正・機構追加・§3 の選択変更のあとは、前提が変わった否定を再走させる**
  (否定は永久ではない。ahc068 では否定済み実験の1本の再走が**単一最大の +49M**。記録していても
  再監査しなければ死蔵になる)。「機構が悪い」と「自分の実装が悪い」を切り分けずに否定として確定しない
  (ahc068: 却下率 85% の原因は機構ではなく制約の張り忘れだった)。
- **枠組みが反証されたら、`LOG.md` を凍結して `LOG2.md` を新設する**(`cp LOG.md LOG2.md` してから書き替える)。
  **凍結した旧ログの冒頭に「以後の前提として再利用してはいけない結論」の表を置く**
  (否定された結論が新しい推論に紛れ込むのを防ぐ)。**用語集(§0)は新ログにコピーして引き継ぐ**
  (用語自体は無効にならない)。
- **コミットのたびに `LOG.md §8 変更履歴` に1行追記。**

## 知見の追記(コツ・解法)

- 他人発の知見は**自分の言葉で言い換え＋出典リンク**で載せる。アイデア/技法は著作権対象外だが、
  他者の**文章・図・コード片の逐語コピーは避ける**(本テンプレは CC0。第三者素材は再ライセンスできない)。
- やむを得ず引用する時は短く・出典明記・「CC0対象外」と注記する。

## git

- **`git add` は関係ファイルのみ**(無関係な変更を巻き込まない)。破壊的/外向きの操作(push 等)は指示があるまでしない。
- **この contest repo は private 前提**。`problem/` に AtCoder の問題文(著作物)を追跡するため public 化しない。
  public への push/公開を求められたら、再配布になる旨を指摘して確認を取る(テンプレ `ahc-template` のみ public)。
