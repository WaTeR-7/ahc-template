# AHC<XXX>「<問題名>」— プロジェクトログ

> コンテキスト圧縮 / AI 引き継ぎ用の継続ログ。**コミットのたびに §9 変更履歴に1行追記。**
> コンテスト: <名称>, <日程>。
>
> **前提（§3 register の値や §7 の発見）が反証された時**: 古い結論を消さず、この LOG.md を凍結して
> `LOG2.md` を新設する（`cp LOG.md LOG2.md` してから書き替える）。**凍結した旧ログの冒頭に「以後の前提として
> 再利用してはいけない結論」の表を置く**（否定された結論が新しい推論に紛れ込むのを防ぐ）。新ログの
> §0 用語集は旧ログからコピーして引き継ぐ（用語自体は無効にならない）。

---

## 0. 用語集（**冒頭に置く。長いセッションで一番参照される節**）

> 長いセッションでは自分たちで作った略語・記号（Φ, gphi, floor, ride 等）が積み重なり、その都度説明を
> 求めているとロスになる。**新しい用語を作ったらここに追記する**（思いついた本人が一番正確に書ける）。
> 「この用語なに？」と聞く/聞かれる前に、まずここを見る/見せる。

| 用語 | 意味 | 初出（コミット/ファイル） |
|---|---|---|
| | | |

## 1. 序盤チェックリスト（この順で。過去の反省を制度化）

- [ ] **1手の最大威力を制約の条文から読み直す**（実行可能解を書く*前*に。「自然に思える操作」ではなく
      **制約が許す最も強い操作**を primitive にする。**パラメータの端**（幅1 / 最小サイズ / 上限いっぱい /
      degenerate な形）が合法かを条文で1つずつ確認する ── AHC068 は「矩形の幅は 1 でよい」の一点で
      1手の威力が **4倍**違い、そこに気づかないまま積んだ全レバーが無駄になった）。
- [ ] **まず実行可能解**（貪欲/構築で1つ。以後 best-of で絶対に退行させない）。
- [ ] **§3「設計上の選択」register を埋める**（実行可能解の直後。自分が何を*選んだ*のかを書き出す。
      以後、行き詰まったら必ずここへ戻る ── **負の連続は収束ではなく、共通の上流前提を疑う合図**）。
- [ ] **赤字の構造を測る**（`scripts/measure.py`）。「どのケース群で・なぜ負けているか」を特徴量×成績の
      相関/バケットで把握 → **効果配分をここで決める**。自分の分析が「小レバー」と言った所を磨かない。
      （既定は seed+score。**手数/誤差など問題固有の指標を CSV 列に足せば詳細分析できる** ── measure.py 参照）
- [ ] **上位との差を逆算**（順位表/スコアから想定指標を逆算し、狙う水準を知る）。
- [ ] **自分の解法クラスの天井を「スコアの単位」で計算する**（1手で進める量から理論下限を出し、
      それをスコア式に通す。**単位はスコア式に直接入る量で揃える。距離など代理量で出すと嘘の下限になる**）。
      **上位スコアが自分の天井を超えていたら、磨いても届かない ⇒ 解法クラスごと乗り換える。**
      （AHC068: `floor=Σceil(|Δ|/10)` ⇒ 2.345手/枚 ⇒ 「1手1枚」クラスの天井 est 1011M。
      上位は 1.87手/枚 = 1059M で**天井を割っていた** ⇒ 1手で複数枚を進める機構が数学的に必須と確定した。）
      結果は §7 に記録する（以後「このクラスで磨くか / 乗り換えるか」の判断が全部これに乗る）。
- [ ] **評価関数は測って選ぶ**（特徴量と残りコストの相関。当てずっぽうにしない）。
- [ ] **ボトルネックを一度プロファイル**（`lib/prof.rs` を貼って `ON=true` でビルド。**回数 × 1回のコスト**を
      両方出す。合計の 90% 以上を占める段が1〜2個に絞れるまで細分する）。実装最適化はアルゴリズム変更と
      直交する大レバーになり得る。**推測で削らない**（ahc068: 1つのホットループの中に O(N) 19.7M回 と
      O(1) 25.3M回 が同居していて、間引くべき側は計測しないと見えなかった ── 特定できたら1行で時間が半分に）。
- [ ] **大きな実装の前に上限を安く測る**（時間N倍=スループットN倍 等で「このレバーの伸びしろ」を先に見る。
      上の「クラスの天井」は理論下限、こちらは実測の上限 ── 両方あると投資判断が一意に決まる）。

> このセッションの教訓: **測定優先＋構造から考える**を1時間目から。小手先のパラメータ調整でなく、
> 学びのある本質的な探求を。否定的結果は次の問いを鋭くする(捨てず記録)。

## 2. 問題

- <盤面/入力/出力の要約、制約(時間/メモリ)、スコア式。最小化か最大化か等の"本質"を1段落で。>

## 3. 設計上の選択（register。**測定事実ではない ＝ 全て変えられる**）

> **この表がこの雛形で一番効く。** 実装の中には「測って選んだこと」と「最初にそう書いたから今もそうで
> あること」が混ざっている。後者は共通部品に昇格して**問題の性質の語調で書かれる**ため、どの A/B 比較にも
> 写らず、比較ベースの計測規律をすり抜け続ける。
> AHC068 では配達の primitive（隣接スワップ）がこれで、上に積んだ**5つのレバーが全滅した**のを
> 「近最適に収束した」と読んだ。実際は全部が**1つの未検査の選択の下流**で、後にそこが 4 倍の差だと判明した
> （**レバー全滅は収束の証拠ではなく、共通の上流前提を疑う合図**）。
>
> - **書く時**: 最初の実行可能解ができた直後に一度埋める。以後アプローチを足すたびに1行追記。
> - **読む時**: ①レバーが連続して負に出た時 ②上位との差が説明できない時 ③行き詰まった時。
> - **問い**: **「全実験を通して一度も変えていない選択はどれか」**（`変えた実績` が空の行）。
>   それを上流から1本ずつ壊す実験を立てる。**「変えられない」と思っている行こそ最優先。**

| # | 選択（何を決めているか） | 現在の値 | 根拠 | 変えた実績 |
|---|---|---|---|---|
| C1 | **1手で何ができるか**(出力の最小単位) | <制約が許す最強の形になっているか> | 惰性 | — |
| C2 | 解の表現(内部状態の持ち方) | | 惰性 | — |
| C3 | 何を最小化/最大化しているか(スコアの代理) | | 惰性 | — |
| C4 | 構築順序(どれから決めるか) | | 惰性 | — |
| C5 | 決めたものを後で動かすか(不可侵か) | | 惰性 | — |
| C6 | 候補生成の粒度(近傍/遷移の定義) | | 惰性 | — |
| C7 | 探索の枠組み(貪欲/ビーム/焼きなまし/DP) | | 惰性 | — |
| C8 | 時間の使い方(1本を深く / 多スタート best-of) | | 惰性 | — |

- `根拠` は **測定 / 直感 / 惰性**(最初の実装のまま)の3値で書く。**「惰性」かつ「変えた実績が空」が容疑者。**
- 行は問題に合わせて足す/割る。**「これは問題の性質だから変えられない」と書きたくなったら、制約の条文に
  戻って確認する**（特に C1 ── §1 の1項目目。ここを外すと、上に積んだレバーが全部無駄になる）。
- 変えた選択は、正なら §6、負なら **§7a の棚**（再監査の対象）に書いて `変えた実績` に印をつける
  （**負の記録が次の容疑者を絞る。ただし負は「その時の実装での負」なので永久ではない**）。

## 4. リポジトリ / 実行

- ソルバ: 外部クレート非依存の単一 `src/bin/NN_<name>.rs`(提出はこの1ファイルを貼る)。
- **セットアップ(人手)**: ① 本コンテストの AI 利用規約を `problem/ai_guideline.txt` に貼る(空だと AI は全停止)
  ② 問題ページ(要ログイン+参加登録)を「完全な形で保存」→ `problem/` に置く
  → 以降 `scripts/fetch_tools.sh` が保存HTML内のCDN URLから tools.zip をDL→展開→build→`in/`生成まで自動。
- 公式ツール: `tools/`(gitignore)。より多くの入力は `tools/` で `cargo run -r --bin gen seeds.txt`。採点は `scripts/test.sh` の `SCORER` を編集。
- 掃引: `scripts/test.sh <bin> [num] [KEY=val ...]`（`results.csv` = seed,score,ms /
  `results.meta` = サマリで**存在＝完了の目印**。無効解閾値は `INVALID_BELOW`）。計測: `scripts/measure.py [results.csv]`。
- byte 一致照合: `A_ENV="AHC_NEW=0" scripts/same.sh <新bin> <前bin> [num]`（決定性の自己チェック付き）。
- ビジュアライザ: <URL>。提出URL: <URL>、言語 Rust。

## 5. 作業ルール

- **1アプローチ=1物理ファイル**(`NN_<name>.rs`)。過去の版を絶対に上書きしない。
- **再利用部品は `lib/` に種類ごと分割**(`mod` ブロック)。cargo の lib ターゲット `ahc_lib` なので `cargo check`/`test` で検証。解答へは `use` せずコピペ(自己完結・そのまま提出可・bundler不使用)。
- **こまめにコミット**、メッセージにローカル合計スコアを記録。ベスト版を常に復元可能に(実質 main 一本で良い)。
- **ブランチは任意**(必須ではない)。捨てる前提の大改造で WIP を main に残したくない時だけ切る。
  復元性は上のファイル分割＋スコア入りコミットが担保する(AHC は直列・加算的で分岐戦略は効きにくい)。
- **env チューナブル**(AHC_TL 等)で再コンパイル無しに掃引。
- **ローカルは遅い前提**。時間予算は上限近く(自己制限は速いジャッジに自動追従)。
- **否定結果は前提つきで §7a に積み、バグ修正/機構追加/選択変更のあとに再走させる**(否定は永久ではない)。
- **検証の作法**: ①新機構は「無効化した設定で前版と byte 一致」を確認してから有効化
  (`A_ENV="AHC_NEW=0" scripts/same.sh <新> <前版> 20`) ②**出力を1手ずつ検査**(`AHC_VERIFY=1`。
  終状態の検査では足りない ── 内部が解けていても提出が違法なことがある) ③**壁時計は最外の締切だけ**、
  内部段は `timer::Budget`(作業量カウンタ)で打ち切る(壁時計だと解が非決定的になり比較が壊れる)。

## 6. 試したアプローチ

| # | アプローチ | ファイル | ローカル結果 | 判定 |
|---|---|---|---|---|
| 0 | ベースライン(構築のみ) | `00_base.rs` | — | — |

- `判定` が負のものは **§7a の棚に「前提つき」で載せる**（後で再監査するため。負のまま流すと死蔵になる）。
- 比較は**同一 seed 集合・同一 env・同一時間予算**で（`results.meta` に両方記録される）。
  **自分のパラメータで最適化済みの変種を、最適化していないベースラインと比べない。**
- **併用は net で測る**。レバーは時間予算を食い合うので、**単独の +X を足し算しない**
  （例: A単独 +X、B単独 +Y でも A+B が X+Y になるとは限らない。組み合わせで seed 掃引をやり直す）。
- **「小さいレバー」と判定する前に実測する**。壁時計や暗算での見積もりは外れる
  （ahc068: root 本数を「プロセス全体の壁時計 ÷ 1本」で概算し実測の 1/3 以下に見誤った。
  カウンタを入れてから語る）。

## 7. 主要な発見 / 分析

- <引き継ぎ用に必読の発見を箇条書きで。特に"何がボトルネックで、なぜ"を。>

### 7a. 否定結果の棚（**再監査の対象**。捨てずにここへ積む）

> **否定結果は「その時の実装での」否定にすぎない。** バグ修正・機構追加・§3 の選択の変更のあとは、
> 前提が変わった否定を**必ず再走させる**。AHC068 では、狭めの実装後に「既に否定済み」だった実験を
> 1つ再走させただけで **+49M**（セッション単一の最大ジャンプ）。
> **否定を再監査しないことが最大の機会損失だった**（否定を記録しても、再監査しなければ死蔵になる）。

| # | 否定したこと | 測定条件（どの版・どの機構が有効/無効だったか） | これが無効になる条件 | 再監査 |
|---|---|---|---|---|
| N1 | | | | 未 |

- 記録するのは結論だけでなく**前提**。「A は効かない」ではなく「**B が無い状態では** A は効かない」と書く。
- `これが無効になる条件` が書けない否定は、まだ原因が分かっていない ⇒ 切り分けてから棚に載せる
  （分からないまま「効かない」と書くと、二度と触れない領域が増えるだけ）。
- 再監査のタイミング: ①バグ修正の直後 ②新機構が入った直後 ③§3 の選択を変えた直後 ④行き詰まった時。
- **自分の実装のバグで負が出ていた**ケースが実際にある（ahc068: 却下率 85% の原因は機構ではなく
  制約の張り忘れ）。「機構が悪い」と「実装が悪い」を切り分けずに棚へ載せない。

## 8. 現行ベスト & 次の一手

- 現行ベスト: <file> / est(最終ケース換算) <値> / 提出 <値>。次: <レバー>。

### 8a. 実ジャッジ校正（提出のたびに追記）

> ローカル推定(est)は実ジャッジと系統的にずれるが、**差分は忠実に転写される**ことが多い
> (ローカルで +14.63M の変更が実ジャッジで +18.7M として現れる、等)。3回分たまると「est − N」の形の
> 予測式になり、次の提出前に実スコアの見込みが立つ。

| 提出 | ローカル est | 実ジャッジ | 乖離 | ローカル maxms | 実 ms |
|---|---|---|---|---|---|
| | | | | | |

- 乖離が縮み続ける/一定なら見積り式として使ってよい。急に乖離が変わったら**測定条件(seed集合/env/TL)が
  ずれていないか**を先に疑う。
- 時間(ms)もローカル/実で突き合わせる。大きくずれるなら「ローカルは遅い前提」という CLAUDE.md の
  仮定そのものを疑う(逆に速いジャッジに当たっている可能性もある)。

## 9. 変更履歴（最新を末尾に、1コミット1行）

- `<hash>` 初期セットアップ(テンプレから scaffold)。
