# AHC<XXX>「<問題名>」— プロジェクトログ

> **この LOG は「このコンテストの記録」だけを持つ。方法論・作業ルール・過去の失敗例は `CLAUDE.md`。**
> （同じことを両方に書くと必ず片方が古くなる。ルールを足したくなったら CLAUDE.md へ。）
> コンテスト: <名称>, <日程>。**コミットのたびに §8 に1行追記。**
> 枠組みが反証されたら、この LOG を凍結して `LOG2.md` を新設する（手順は CLAUDE.md）。

| 節 | 書くもの | 更新するとき |
|---|---|---|
| §0 用語集 | 自分たちで作った略語・記号 | 用語を作った直後 |
| §1 序盤チェックリスト | 序盤の進捗（済/未） | 序盤 |
| §2 問題 | 制約・スコア式・本質 | 最初に1回 |
| §3 設計上の選択 | **何を選んだか**（測定事実ではない ＝ 全て変えられる） | 選択を足す/変えるたび |
| §4 リポジトリ / 実行 | URL・コマンド・このコンテスト固有の運用 | 最初に1回 |
| §5 試したアプローチ | 実装したものと結果 | アプローチごと |
| §6 発見 / 分析（+6a 否定結果の棚） | 測って分かったこと / 効かなかったこと | 測るたび |
| §7 現行ベスト（+7a 実ジャッジ校正） | 今の最良と次の一手 / 提出の実績 | 提出ごと |
| §8 変更履歴 | 1コミット1行 | コミットごと |

---

## 0. 用語集

> 自分たちで作った略語・記号（Φ, floor, ride 等）は**作った直後にここへ**。長いセッションでは
> 「この用語なに？」の往復が積み重なってロスになる。**新ログを立てる時もこの節はコピーして引き継ぐ。**

| 用語 | 意味 | 初出（コミット/ファイル） |
|---|---|---|
| | | |

## 1. 序盤チェックリスト

> **各項目の根拠・失敗例は CLAUDE.md「進め方」にある。ここは進捗の欄。**

- [ ] **1手の最大威力を制約の条文から読み直す**（実行可能解を書く*前*に。パラメータの端が合法かを1つずつ）→ §3 C1
- [ ] **まず実行可能解**（貪欲/構築で1つ。以後 best-of で絶対に退行させない）
- [ ] **§3 register を埋める**（自分が何を*選んだ*のかを書き出す）
- [ ] **赤字の構造を測る**（`scripts/measure.py`。どのケース群で・なぜ負けているか ⇒ **効果配分をここで決める**）→ §6
- [ ] **上位との差を逆算**（順位表/スコアから想定指標を逆算し、狙う水準を知る）→ §2
- [ ] **解法クラスの天井をスコアの単位で計算**（代理量で出すと嘘の下限になる）→ §6
- [ ] **評価関数は測って選ぶ**（特徴量と残りコストの相関）
- [ ] **ボトルネックを一度プロファイル**（`lib/prof.rs`。回数 × 1回のコストを両方）→ §6
- [ ] **大きな実装の前に上限を安く測る**（時間N倍などで、そのレバーの伸びしろを先に見る）

## 2. 問題

- <盤面/入力/出力の要約、制約(時間/メモリ)、スコア式。最小化か最大化か等の"本質"を1段落で。>
- 狙う水準: <上位スコアから逆算した想定指標>。解法クラスの天井: <スコアの単位で計算した理論下限>。

## 3. 設計上の選択（register）

> **問い: 「全実験を通して一度も変えていない選択はどれか」**（`変えた実績` が空の行）。
> それを上流から1本ずつ壊す実験を立てる。**「変えられない」と思っている行こそ最優先。**
> なぜこの表が効くのか / 過去にこれを外して何が起きたかは CLAUDE.md。

| # | 選択（何を決めているか） | 現在の値 | 根拠 | 変えた実績 |
|---|---|---|---|---|
| C1 | **1手で何ができるか**(出力の最小単位) | | 惰性 | — |
| C2 | 解の表現(内部状態の持ち方) | | 惰性 | — |
| C3 | 何を最小化/最大化しているか(スコアの代理) | | 惰性 | — |
| C4 | 構築順序(どれから決めるか) | | 惰性 | — |
| C5 | 決めたものを後で動かすか(不可侵か) | | 惰性 | — |
| C6 | 候補生成の粒度(近傍/遷移の定義) | | 惰性 | — |
| C7 | 探索の枠組み(貪欲/ビーム/焼きなまし/DP) | | 惰性 | — |
| C8 | 時間の使い方(1本を深く / 多スタート best-of) | | 惰性 | — |

- `根拠` は **測定 / 直感 / 惰性**(最初の実装のまま)の3値。**「惰性」かつ「変えた実績が空」が容疑者。**
- 行は問題に合わせて足す/割る。**「問題の性質だから変えられない」と書きたくなったら条文に戻って確認する。**
- 変えたら `変えた実績` に印をつけ、結果は正なら §5、負なら §6a へ。

## 4. リポジトリ / 実行

- ソルバ: 外部クレート非依存の単一 `src/bin/NN_<name>.rs`(提出はこの1ファイルを貼る)。
- 公式ツール: `tools/`(gitignore。無ければ `scripts/fetch_tools.sh`)。より多くの入力は `tools/` で
  `cargo run -r --bin gen seeds.txt`。採点器は `scripts/test.sh` の `SCORER` を編集。
- 掃引: `scripts/test.sh <bin> [num] [KEY=val ...]`（`results.csv` = seed,score,ms /
  `results.meta` = サマリで**存在＝完了の目印**。無効解閾値は `INVALID_BELOW`）。計測: `scripts/measure.py [results.csv]`。
- byte 一致照合: `A_ENV="AHC_NEW=0" scripts/same.sh <新bin> <前bin> [num]`（決定性の自己チェック付き）。
- ビジュアライザ: <URL>。提出URL: <URL>、言語 Rust。
- **このコンテスト固有の運用**: <CLAUDE.md の一般ルールから逸脱する点だけ書く。例: 提出が手動、
  対話問題、TL が特殊、ジャッジ速度がローカルと大きく違う、など>

## 5. 試したアプローチ

| # | アプローチ | ファイル | ローカル結果 | 判定 |
|---|---|---|---|---|
| 0 | ベースライン(構築のみ) | `00_base.rs` | — | — |

- `判定` が負のものは **§6a の棚へ**（前提つきで書く。負のまま流すと死蔵になる）。
- 結果は `results.meta` の `bin / env / seeds` ごと引用する（比較の作法は CLAUDE.md）。

## 6. 主要な発見 / 分析

- <引き継ぎ用に必読の発見を箇条書きで。特に"何がボトルネックで、なぜ"を。>

### 6a. 否定結果の棚（**再監査の対象**）

> **否定結果は「その時の実装での」否定にすぎない。** バグ修正・機構追加・§3 の選択変更のあとは、
> 前提が変わった否定を再走させる（再監査しないと記録は死蔵になる。理由と実例は CLAUDE.md）。

| # | 否定したこと | 測定条件（どの版・どの機構が有効/無効だったか） | これが無効になる条件 | 再監査 |
|---|---|---|---|---|
| N1 | | | | 未 |

- 結論ではなく**前提つき**で書く。「A は効かない」ではなく「**B が無い状態では** A は効かない」。
- `これが無効になる条件` が書けない否定は原因が分かっていない ⇒ 切り分けてから載せる
  （分からないまま「効かない」と書くと、二度と触れない領域が増えるだけ）。

## 7. 現行ベスト & 次の一手

- 現行ベスト: <file> / est(最終ケース換算) <値> / 提出 <値>。次: <レバー>。

### 7a. 実ジャッジ校正（提出のたびに追記）

| 提出 | ローカル est | 実ジャッジ | 乖離 | ローカル maxms | 実 ms |
|---|---|---|---|---|---|
| | | | | | |

- **est の絶対値は系統的にずれるが、差分は忠実に転写される**ことが多い。3回たまれば「est − N」の形で
  次の提出の見込みが立つ。乖離が急に変わったら、まず**測定条件(seed集合/env/TL)のずれ**を疑う。
- 時間(ms)も突き合わせる。大きくずれるなら「ローカルは遅い前提」という仮定自体を疑う。

## 8. 変更履歴（最新を末尾に、1コミット1行）

- `<hash>` 初期セットアップ(テンプレから scaffold)。
